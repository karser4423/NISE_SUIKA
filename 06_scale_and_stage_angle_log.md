# サイズとステージ角度変更記録

## 2026-06-09

### 変更内容

- ドリンクオブジェクトの半径を前回値から約15%縮小。
- サイズ範囲を `90px` から `220px` ではなく、`76px` から `187px` に変更。
- ステージ奥側の絞り量を幅の `23%` から `13%` に変更。
- 描画上の台形と、左右壁の衝突判定の両方で同じ絞り量を使うよう維持。

### 変更理由

- 前回の5倍サイズではステージに対して少し大きすぎたため。
- ステージの奥行きパースが強く、角度が立って見えすぎていたため、より水平に近い見た目へ調整するため。
